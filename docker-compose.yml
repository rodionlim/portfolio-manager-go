services:
  portfolio-manager:
    image: portfolio-manager:latest  # Use the locally built image
    build: .  # Or build from Dockerfile if SSL issues are resolved
    ports:
      - "8080:8080"  # HTTP API
      - "8081:8081"  # MCP server
    volumes:
      # Mount your custom config file
      - ./config.yaml:/app/config/config.yaml:ro
      # Mount data directory for persistence
      - portfolio-data:/app/data
      # Mount logs directory
      - portfolio-logs:/app/logs
    # environment:
      # Set environment variables here if needed
      # GEMINI_API_KEY: "your_api_key_here"
      # ANALYTICS_SCHEDULE: "0 17 * * 1"
    restart: unless-stopped

volumes:
  portfolio-data:
    driver: local
  portfolio-logs:
    driver: local